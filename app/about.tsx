import React from 'react'
import { Text, StyleSheet } from 'react-native'

const About = () => {
  return <Text style={styles.container}>about page</Text>
}

const styles = StyleSheet.create({
  container: {
    padding: 10,
  },
})

export default About
